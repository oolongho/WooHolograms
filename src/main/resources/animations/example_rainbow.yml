# 自定义动画示例配置
# 动画名称为文件名（不含 .yml 扩展名）

# 动画速度（tick）
speed: 5

# 动画暂停时间（tick，可选）
pause: 0

# 动画帧列表
# 使用 {text} 占位符表示原始文本
frames:
  - "&a{text}"
  - "&b{text}"
  - "&c{text}"
  - "&d{text}"
  - "&e{text}"
